import pandas as pd


def preprocess_data_01(df: pd.DataFrame) -> pd.DataFrame:
    return df


def preprocess_data_02(df: pd.DataFrame) -> pd.DataFrame:
    return df


def preprocess_data_03(df: pd.DataFrame) -> pd.DataFrame:
    return df
